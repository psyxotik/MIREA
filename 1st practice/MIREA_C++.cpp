#include <iostream>
#include <math.h>

using namespace std;
int main() {
    setlocale(LC_ALL, "RU");
    //#1 Выведите на экран вашу Фамилию, Имя и номер студенческой группы.
    cout << "Тажетдинов Равиль, БФБО-07-24" << endl;

    //#2 Напишите программу, которая запрашивает у пользователя радиус круга и, используя полученный радиус, вычисляет площадь круга.
    //Функцию степени мы изучим в следующих тетрадях, используйте произведение переменных.
    int r;
    float pi = 3.14f;
    cout << "Введите радиус круга: ";
    cin >> r;
    cout << "Площадь круга радиусом " << r << " равна " << pi * r * r << endl;
    //Напишите программу для решения логического выражения: A<0 ИЛИ (B>89 И (C > 0 ИЛИ D==10))
    // при начальных данных: A=12; B=90; C=-9; D=10. Выведете результат на консоль. Объясните вывод логического выражения. 
    int A{12}, B{90}, C{-9}, D{10};
    if (A < 0 || (B > 89 && (C > 0 || D == 10))) cout << "true" << endl;
    else cout << "false" << endl;
    //Напишите программу, которая предлагает ввести два целых числа,
    // а затем использует конструкцию if-else для вывода сообщения о том, равны ли два числа.
    int a_1, b_1;
    cout << "Введите число a" << endl;
    cin >> a_1;
    cout << "Введите число b" << endl;
    cin >> b_1;
    if (a_1 == b_1) cout << "Числа равны" << endl;
    else cout << "Числа не равны" << endl;
    //Напишите программу, которая предлагает ввести два целых числа и выясняет,
    // делится ли первое число на второе без остатка (кратное второму числу).
    // Предусмотрите вариант, когда в качестве второго числа можно ввести 0 (на ноль делить нельзя).
    // В этом случае программа ничего не вычисляет, а просто завершает

    int num_1, num_2;
    cout << "Введите число a" << endl;
    cin >> num_1;
    cout << "Введите число b" << endl;
    cin >> num_2;
    if (num_2 != 0 && num_1 % num_2 == 0) cout << "Число " << num_1 << " делится на число " << num_2 << " без остатка" << endl;
    else if (num_2 == 0) cout << "На ноль делить нельзя!" << endl;
    else cout << "Число " << num_1 << " не делится на число " << num_2 << " без остатка" << endl;

    //Дано натуральное число n. Определите, является ли оно степенью числа 2,
    // и выведите слово YES, если является и слово NO, если не является.
    int n_1, tmp;
    cout << "Введите число n" << endl;
    cin >> n_1;
    tmp = n_1;
    while (n_1 % 2 == 0) n_1 /= 2;
    if (n_1 == 1) cout << "Число " << tmp << " является степенью двойки" << endl;
    else cout << "Число " << tmp << " не является степенью двойки" << endl;

    //Напишите программу, которая запрашивает у пользователя положительное целое число и проверяет,
    // является ли оно простым. Программа должна продолжать запрашивать ввод,
    // пока не будет введено отрицательное число.
    int prime_num;
    cout << "Введите положительное целое число (отрицательное для выхода): ";
    cin >> prime_num;
    while (prime_num >= 0) {
        bool is_prime = true;
        if (prime_num < 2) is_prime = false;
        for (int i = 2; i <= sqrt(prime_num); i++) {
            if (prime_num % i == 0) {
                is_prime = false;
                break;
            }
        }
        if (is_prime) cout << prime_num << " является простым числом." << endl;
        else cout << prime_num << " не является простым числом." << endl;
        cout << "Введите положительное целое число (отрицательное для выхода): ";
        cin >> prime_num;
    }
    //Напишите программу, которая выполняется пока переменная s больше 2n. Если s меньше 2n, производятся следующие вычисления:
    //• Переменная s уменьшается на 30;
    //• Переменная n увеличивается на 20.
    //Начальные значения s и n равны 900 и 30 соответственно.
    int s = 900, n = 30;
    while (s > 2 * n) {
        s -= 30;
        n += 20;
    }
    cout << "Значение s: " << s << ", Значение n: " << n << endl;

    //Напишите программу, которая выводит квадраты нечетных целых чисел от 1 до предела, который вводит пользователь.
    int limit_1;
    cout << "Введите предел: ";
    cin >> limit_1;
    cout << "Квадраты нечетных целых чисел от 1 до " << limit_1 << ":" << endl;
    for (int i = 1; i <= limit_1; i += 2) {
        cout << i * i << " ";
    }
    cout << endl;
    //Вычислить 
    double summ = 1, limit;
    cout << "Введите предел: ";
    cin >> limit;
    for (double x = 1; x < limit; x++){
        summ *= 1/(x + x * x);
    } 
    cout << "Произведение " << limit << " чисел равно: " << summ << endl;

    //Напишите программу, которая запрашивает у пользователя 10 целых чисел и вычисляет их среднее арифметическое.
    int numbers[10];
    float sum = 0;
    cout << "Введите 10 целых чисел:" << endl;
    for (int i = 0; i < 10; i++) {
        cin >> numbers[i];
        sum += numbers[i];
    }
    cout << "Среднее арифметическое: " << sum / 10 << endl;

    //Напишите программу, которая находит минимальное число x, в результате ввода которого при выполнении цикла a будет равно 2, b станет равно 13:
    //while (x > 0) {
        //a = a+1;
        //b = b + (x%100);
        //x = x/100;
    // }
    int a = 0, b = 0, x;
    cout << "Введите значение x: ";
    cin >> x;
    while (x > 0) {
        a = a + 1;
        b = b + (x % 100);
        x = x / 100;
    }
    cout << "a: " << a << ", b: " << b << endl;

    return 0;
}